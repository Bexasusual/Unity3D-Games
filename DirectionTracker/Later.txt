1. 读写文件，可以载入关卡并根据关卡来决定级别；
2. Button生成4个选项，一个正解，两个接近项，一个随机项（可能很离谱）；
3. 点击按钮实现继续游戏；

注意：
1. AnswerCanvas按钮排序为顺时针，Box安排类似；
2. 函数名统一大写，更改脚本中的引用名后记得重新拖拽赋值；

实现：
1. GameManager检测鼠标点击，判断是否执行事件；

优化：
1. 在Cube四围标上序号，提问时显现；
2. 优化按钮显示；




进度：
1. 实现了方块的旋转；
2. 实现了按钮文字的改变与显现，以及点击后的参数传递；

8.28更新
3. 点击事件统一由GameController控制，1表示正等待旋转；
下一步是点击按钮后判断正误并显现，同时状态应改为1（显示值）；
之后考虑计时计分，生命显示，*关卡增加*，以及音乐搭配等；

问题：
1. 数组的赋值在重启后会丢失；
2. 序号显示不明显；
3. 旋转时点击鼠标，会加速旋转


变种：
1. 旋转时向邻居增值，自身减值（相当难）
2. 旋转只与自身有关，右转增值，左转减值（难）
3. 旋转的内部是个方向标（中等）
PS：1，2玩法如果考虑负数，则会更难；

初代：
1. 增加BGM，有办法再加效果音；
2. 调整提示正误的Text色彩等，调整Question的位置和颜色；


